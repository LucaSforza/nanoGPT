[project]
name = "nanoGPT"
version = "0.1.0"
description = "Simple GPT training and inference"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy",
    "tiktoken",
    "tqdm",
    "wandb",
    "transformers",
    "datasets",
]
